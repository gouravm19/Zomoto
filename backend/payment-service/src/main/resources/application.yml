spring:
  application:
    name: payment-service
server:
  port: 8084
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
