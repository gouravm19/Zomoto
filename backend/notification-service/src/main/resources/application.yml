spring:
  application:
    name: notification-service
server:
  port: 8086
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
