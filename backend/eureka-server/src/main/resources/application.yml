spring:
  application:
    name: eureka-server
server:
  port: 8761
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
