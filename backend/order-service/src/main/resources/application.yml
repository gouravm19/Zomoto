spring:
  application:
    name: order-service
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
