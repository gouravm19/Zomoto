spring:
  application:
    name: search-service
server:
  port: 8087
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
